<%= form_for(@geosmarker, :html => {:multipart => true}) do |f| %>
  <% if @geosmarker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@geosmarker.errors.count, "error") %> prohibited this geosmarker from being saved:</h2>

      <ul>
      <% @geosmarker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
    <%= f.label :id %>
    <%=  @geosmarker.id %>
  </div>
<div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>    
  <div class="field">
    <%= f.label :lat %>
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :lng %>
    <%= f.text_field :lng %>
  </div>
    <div class="field">
    <%= f.label :created_at %>(0-21)
    <%=  @geosmarker.created_at %>
  </div>
    <div class="field">
    <%= f.label :updated_at %>(0-21)
     <%=  @geosmarker.updated_at %>
  </div>
  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>
  <div class="actions">
    <%= submit_or_cancel(f) %>
  </div>
<% end %>
